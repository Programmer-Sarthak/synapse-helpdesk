Start from develop: git checkout develop

Make a feature branch: git checkout -b feature/dark-mode

Write your code.

Save your work: git add . then git commit -m "feat: added dark mode toggle"

Merge it to develop for testing:

git checkout develop

git merge feature/dark-mode

When it is tested and perfect, merge to live:

git checkout main

git merge develop

git push origin main